{
  "name": "claude-code-settings-schema",
  "version": "0.0.4",
  "description": "Generate JSON Schema for Claude Code settings.json files",
  "main": "dist/cli.js",
  "bin": {
    "claude-code-settings-schema": "dist/cli.js",
    "ccss": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "tsc",
    "generate": "node dist/cli.js",
    "prepublishOnly": "pnpm build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm run build && changeset publish"
  },
  "keywords": [
    "claude",
    "claude-code",
    "json-schema",
    "settings"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^24.10.1",
    "prettier": "^3.7.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "typescript-json-schema": "^0.67.0"
  }
}